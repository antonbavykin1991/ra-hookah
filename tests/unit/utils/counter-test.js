import counter from 'ra/utils/counter';
import { module, test } from 'qunit';

module('Unit | Utility | counter', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = counter();
    assert.ok(result);
  });
});
